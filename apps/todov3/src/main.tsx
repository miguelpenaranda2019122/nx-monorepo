import { AppRegistry } from 'react-native';
import App from './App';
import { name } from '../app.json'


// after copy - Change the name for the correct
AppRegistry.registerComponent(name, () => App);
